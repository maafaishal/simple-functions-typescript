{
  "name": "simple-functions-typescript",
  "version": "1.0.0",
  "description": "A collection of functions in Typescript",
  "main": "index.js",
  "author": "maafaishal",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "typescript": "^3.8.3"
  }
}
